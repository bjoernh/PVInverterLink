# Docker Image Tags Configuration
# Copy this file to .env.image-tags and customize for your deployment strategy
#
# These tags define which Docker image version to pull for each environment.
# The tags correspond to images in the registry: git.64b.de/bjoern/deye_hard

# =============================================================================
# Test Environment
# =============================================================================
# Test environment typically uses the 'test' tag or specific commit SHAs
# for bleeding-edge testing
TEST_IMAGE_TAG=test

# Alternative: Use a specific commit SHA for precise version control
# TEST_IMAGE_TAG=sha-abc1234

# =============================================================================
# Staging Environment
# =============================================================================
# Staging should use the 'staging' tag or release candidate versions
# to test pre-release code before production
STAGING_IMAGE_TAG=staging

# Alternative: Use release candidates for version-specific testing
# STAGING_IMAGE_TAG=v1.2.0-rc.1

# =============================================================================
# Production Environment
# =============================================================================
# Production MUST use stable semantic version tags or the 'prod' tag
# Never use 'latest', 'test', or commit SHAs in production!
PROD_IMAGE_TAG=prod

# Recommended: Use specific semantic versions for production deployments
# PROD_IMAGE_TAG=v1.2.0

# =============================================================================
# Tag Strategy Reference
# =============================================================================
# Available tag formats pushed by CI/CD:
#
# - latest           : Most recent build from main/master branch
# - main             : Latest build from main branch
# - test             : Latest build pushed to test
# - staging          : Latest build pushed to staging
# - prod             : Latest production-approved build
# - v1.2.3           : Semantic version (created from git tags)
# - sha-abc1234      : Specific git commit (all builds)
#
# Tag usage guidelines:
#
# ✅ Test:       test, sha-xxxxx, or branch names
# ✅ Staging:    staging, v1.x.x-rc.1, or sha-xxxxx
# ✅ Production: v1.x.x (semantic version) or prod
# ❌ Production: NEVER use latest, test, or sha-xxxxx in prod!

# =============================================================================
# Deployment Examples
# =============================================================================
#
# Deploy test environment with specific commit:
#   export IMAGE_TAG=sha-abc1234
#   deployment/scripts/deploy-test.sh
#
# Deploy staging with release candidate:
#   export IMAGE_TAG=v1.2.0-rc.1
#   deployment/scripts/deploy-staging.sh
#
# Deploy production with semantic version:
#   export IMAGE_TAG=v1.2.0
#   deployment/scripts/deploy-prod.sh
